# Inxbit Homebrew Tap

Homebrew formulae for Inxbit projects.

## PrismTTY

Install PrismTTY with:

```sh
brew install inxbit/tap/prismtty
```

Or tap this repository first:

```sh
brew tap inxbit/tap
brew install prismtty
```
